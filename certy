BASE=apps.example.com
for H in console-openshift-console.$BASE oauth-openshift.$BASE; do
  echo "=== $H"
  echo | openssl s_client -connect $H:443 -servername $H 2>/dev/null \
    | openssl x509 -noout -subject -issuer -dates || echo "EOF/błąd handshaku"
done


SVCIP=$(oc -n openshift-ingress get svc/router-default -o jsonpath='{.spec.clusterIP}')
H=console-openshift-console.apps.example.com
oc -n openshift-console run -it diag --image=registry.access.redhat.com/ubi9/ubi --restart=Never -- bash -lc "
  microdnf -y install openssl >/dev/null 2>&1 || true
  echo | openssl s_client -connect $SVCIP:443 -servername $H 2>/dev/null | openssl x509 -noout -subject -issuer -dates
"
# Działa tu, a z zewnątrz NIE → wina HAProxy/ścieżki sieciowej.


oc -n openshift-ingress get pods -o wide
ROUTER=$(oc -n openshift-ingress get pods -l ingresscontroller.operator.openshift.io/deployment-ingresscontroller=default -o jsonpath='{.items[0].metadata.name}')
oc -n openshift-ingress logs $ROUTER -c router --tail=200 | egrep -i "proxy|handshake|ssl|error|sni" || true



# Sprawdź typ i zawartość secretu
oc get secret gbw-pki -n openshift-ingress -o yaml

# Wyciągnij certyfikat i sprawdź łańcuch
oc get secret gbw-pki -n openshift-ingress -o jsonpath='{.data.tls\.crt}' | base64 -d | openssl x509 -noout -text | grep -E "Subject:|Issuer:|DNS:|CA:"

# Sprawdź czy to CA czy leaf cert
oc get secret gbw-pki -n openshift-ingress -o jsonpath='{.data.ca\.crt}' | base64 -d | openssl x509 -noout -text 2>/dev/null

# Sprawdź jaki certyfikat faktycznie serwuje serwer SMTP
# Uruchom z poda na nowym klastrze
oc exec <pod> -n <ns> -- bash -c "openssl s_client -connect poczta.moja.domena.pl:587 -starttls smtp"


oc get configmap custom-ca -n openshift-config -o jsonpath='{.data.ca-bundle\.crt}' | \
  awk 'BEGIN{c=0} /BEGIN CERT/{c++} {print > "/tmp/cert"c".pem"}' && \
  for f in /tmp/cert*.pem; do echo "=== $f ==="; openssl x509 -noout -subject -issuer -in $f; done


  # Czy jest jakis volume mount z CA?
oc get deployment <nazwa> -n <ns> -o yaml | grep -A5 -E "volume|secret|configmap" | grep -iE "ca|cert|tls|trust"

# Czy JAVA_OPTS wskazuje na truststore?
oc exec <pod> -n <ns> -- env | grep -iE "java_opts|javax.net.ssl"

# Czy jest jakis init container który importuje certy?
oc get deployment <nazwa> -n <ns> -o yaml | grep -A20 initContainer

oc get secret <nazwa-secretu> -n <namespace> -o go-template='{{range $k,$v := .data}}{{$k}}{{"\n"}}{{$v | base64decode}}{{"\n"}}{{end}}' | \
  awk '/BEGIN CERT/{p=1} p{print} /END CERT/{p=0}' | \
  openssl x509 -noout -subject -issuer -dates